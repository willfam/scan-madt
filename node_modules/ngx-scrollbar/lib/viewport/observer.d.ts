import { Observable } from 'rxjs';
import { ScrollbarUpdateReason } from '../ng-scrollbar.model';
interface ResizeArgs {
    element: HTMLElement;
    throttleDuration: number;
    contentWrapper?: HTMLElement;
}
export declare function resizeObserver({ element, contentWrapper, throttleDuration }: ResizeArgs): Observable<ScrollbarUpdateReason>;
export declare function mutationObserver(element: HTMLElement, throttleDuration: number): Observable<void>;
export {};
